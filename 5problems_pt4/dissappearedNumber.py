"""Find all the elements of [1, n] inclusive that do not
 appear in this array.

Could you do it without extra space and in O(n) runtime? You may 
assume the returned list does not count as extra sp"""

"""def findDisappearedNumbers(self, nums):
        
        missing = set(range(1,len(nums)+1))
        nums = set(nums)
        missing = list(missing.difference(nums))
        return missing""""


    